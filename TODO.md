# TODO

- [ ] Create edit plan for changing resume download buttons so clicking any resume downloads the ATS resume file only (no extra space / no extra ATS button).
- [ ] Update `src/components/sections/Hero.tsx` to remove the second download button and point the remaining resume download to `Gaurav_Sharma_ATS_Resume.docx`.
- [ ] Ensure the download attribute filename matches the ATS file.
- [ ] Run `npm run lint` (and `npm run build` if needed) to confirm no TypeScript/ESLint issues.

